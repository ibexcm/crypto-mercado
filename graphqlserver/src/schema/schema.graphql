# import User from "@ibexcm/database/generated/prisma.graphql"

scalar Upload

type Query {
  user: User!
}

type Mutation {
  sendVerificationCode(args: SendVerificationCodeInput!): Boolean!
  verifyPhoneNumber(args: VerifyPhoneNumberInput!): Session!
  verifyEmail(args: VerifyEmailInput!): Session!
  authenticate(args: AuthenticateInput!): Session!
}

type Session {
  token: String
  expiresAt: DateTime
}

input VerifyEmailInput {
  email: String!
  password: String!
}

input VerifyPhoneNumberInput {
  number: String!
  code: String!
}

input SendVerificationCodeInput {
  number: String!
}

input AuthenticateInput {
  email: String!
  password: String!
}
